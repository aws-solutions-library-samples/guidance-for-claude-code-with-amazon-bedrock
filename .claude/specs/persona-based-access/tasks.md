# Tasks — Persona-Based Access Control & Cost Governance

> Parallel groups; tasks within a group are file-disjoint and run concurrently. Groups are barriers.
> Roles: `[coding]` (Python + Go), `[devops]` (CFN/Lambda wiring/templates). Verification command per task.
> Contracts frozen in `spec.md` §4. All commands run from `source/` (Python) or `source/go/` (Go) unless noted.

Legend: `[ ]` todo · `[~]` in progress · `[x]` done · `[!]` blocked

---

## Group 1: Foundation contracts (front-loaded — unblocks everything)
Spec ref: `spec.md#4` data contracts. Small, fast, disjoint. Everything downstream depends on these landing first.

- [x] [coding] Add persona fields + `effective_auth_type` to Python `Profile` | `source/claude_code_with_bedrock/config.py` | `personas: list[dict]=field(default_factory=list)`, `groups_claim_name: str="groups"`, `fallback_persona: str|None=None` added; `effective_auth_type` property returns "oidc" when `sso_enabled` truthy else "none" (honor future `auth_type` attr if present); `from_dict` preserves persona fields; old profiles load with `personas=[]`. Run: `poetry run pytest tests/test_config.py -q && poetry run ruff check claude_code_with_bedrock/config.py`
  > Done (coding-1). Added `personas`/`groups_claim_name`/`fallback_persona` fields near inference-profile ARNs and the `effective_auth_type` property (honors a future explicit `auth_type`, else maps `sso_enabled`→oidc/none). `from_dict`'s existing field-filter auto-preserves the new fields; old profiles load with `personas=[]`. Added 12 regression tests (TestPersonaFields + TestEffectiveAuthType incl. legacy `sso_enabled`-inference cases). 19 pass, ruff clean. Wrapped 2 PRE-EXISTING E501 long lines in this owned file so the `ruff check` Run passed (behavior-identical). NOTE: poetry was NOT installed on this machine — bootstrapped it into `.poetry-bootstrap/` (python3.10) + in-project `source/.venv` so all Python tasks can verify; PATH-prefix `.poetry-bootstrap/bin` to use it.
- [x] [coding] Add `PersonaConfig` + persona fields to Go `ProfileConfig` | `source/go/internal/config/config.go` | `PersonaConfig` struct exactly per spec §4.2; `Personas []PersonaConfig`, `GroupsClaimName string`, `FallbackPersona string` all `json:"...,omitempty"`; empty slice round-trips; existing fields untouched. Run: `cd source/go && go build ./... && go test ./internal/config/ -count=1`
  > Done (coding-3). `PersonaConfig` struct added byte-for-byte per frozen §4.2 (Name/DisplayName/Group/AllowedModels/DeniedModels/RoleARN/MonthlyTokenLimit/EnforcementMode/CostTags, snake_case json tags + correct omitempty), plus `Personas []PersonaConfig`/`GroupsClaimName`/`FallbackPersona` (all omitempty) on ProfileConfig after FederationType. Existing fields untouched. Added `internal/config/personas_test.go`: absent-fields round-trip (no stray keys), populated round-trip with the 2 reference personas (eng+sales), empty-slice-omitted (reuses existing `contains` helper). `go build ./... && go test ./internal/config/` PASS (`ok ccwb-go/internal/config`). **CORRECTION: my earlier "Go toolchain absent" was a FALSE ALARM** — Go is at `/opt/homebrew/bin/go` (1.26.4, on PATH); my initial `command -v go`/`ls` returned empty (transient shell-init issue). I should have retried before escalating. Verified + completed once confirmed. Parity with Python `Profile` (#4) holds.
- [x] [coding] Add `Claims.GetStringSlice` to Go JWT decoder | `source/go/internal/jwt/decode.go` | new method handles `[]interface{}`→`[]string`, scalar `string`→1-elem slice, missing→nil; stdlib only; existing `GetString`/`GetFloat` untouched. Run: `cd source/go && go test ./internal/jwt/ -count=1`
  > Done (coding-1). Added `func (c Claims) GetStringSlice(key string) []string` to decode.go — type-switch: `[]interface{}`→string elements in order (non-string elems skipped defensively), scalar `string`→1-elem slice, missing/other type→nil. Empty array→non-nil zero-len slice (distinguishes "present but no groups" from absent). Stdlib only, no new deps (cold-start budget). Existing `GetString`/`GetFloat`/`DecodePayload` untouched. Appended 8 tests to existing decode_test.go (array, order-preserved, scalar, missing, mixed-skip, empty-array, wrong-scalar-type, end-to-end via real decoded JWT). 17 jwt tests pass, gofmt+vet clean. Unblocks #12 (credential-process) + #14 (otel-helper).
- [x] [coding] Author shared persona-resolution fixtures | `source/tests/fixtures/persona_resolution_cases.json` | JSON array of cases `{groups, personas(ordered), fallback, expected_name|null}` covering: single match, no-match+no-fallback→null, no-match+fallback, multi-match→first declared, empty personas→null. `[skip-verify]` (data file). Run: `python -c "import json;json.load(open('tests/fixtures/persona_resolution_cases.json'))"`
  > Done (coding-1). 12 generic cases (eng/sales/data — not policy-specific), each `{name, description, groups, personas[ordered], fallback, expected}`. Covers all 5 required cases plus: order-reversed multi-match, empty-groups±fallback, fallback-names-unknown-persona→null, match-beats-fallback, case-sensitive group compare, tolerates-extra-persona-keys. JSON parses; verified all 12 are self-consistent with the §4.3 algorithm so they are a valid parity oracle for #8 (Python) and #9 (Go). Unblocks #8/#9.

> Done when: Python+Go configs carry persona fields (parity), JWT slice accessor exists, fixtures committed. Barrier before Group 2.

---

## Group 2: Resolvers + renderers + JWT wiring (wide parallel)
Spec ref: `spec.md#4.3`, `design.md#2.2`. All file-disjoint; consume Group 1 contracts.

- [x] [coding] Python shared persona resolver | `source/claude_code_with_bedrock/persona_resolution.py` | `resolve_persona(user_groups, personas_ordered, fallback)` per spec §4.3 (declared-order, fallback, None); pure fn; passes all Group-1 fixtures. Run: `poetry run pytest tests/test_persona_resolution.py -q`
  > Done (coding-1). `resolve_persona(user_groups, personas_ordered, fallback)` — pure, stdlib-only; returns the matching persona **dict** (not just the name) per §4.3, declared-order precedence, named-fallback, else None. `user_groups` accepts list or set; group match is exact string equality. Test loads the shared fixtures (#7) and parametrizes all 12 cases + 9 direct unit asserts (returns-object-identity, order precedence, set input, unknown-fallback→None, match-beats-fallback). 21 pass, ruff clean. This is the Python half of the §4.3 parity contract (Go #9 is the other half; #28 parity test will cross-check).
- [x] [coding] Go persona resolver | `source/go/internal/persona/resolve.go` + `resolve_test.go` | `Resolve(groups, personas, fallback)` per §4.3; table tests incl. all fixture cases; declared-order precedence. Run: `cd source/go && go test ./internal/persona/ -count=1`
  > Done (coding-1). New `package persona`: `Resolve(groups []string, personas []config.PersonaConfig, fallback string) (*config.PersonaConfig, error)` — builds a group set, returns the FIRST declared persona whose Group matches (declared-order precedence), else the named fallback if it exists, else `(nil,nil)`. Returns a pointer INTO the slice (so callers read RoleARN). **Parity-critical**: returns `(nil,nil)` for ALL no-result cases (no-match-no-fallback AND unknown-fallback) — never an error — to match Python's `None` over the shared fixtures; the `error` return is reserved per design §2.6, always nil today (caller #12 turns nil→hard-deny). Tests: loads the SAME shared fixtures (#7) via `../../../tests/fixtures/persona_resolution_cases.json` and runs all 12 through Resolve (parity oracle), + a 10-case direct table + pointer-into-slice test. `go test ./internal/persona/` ok, gofmt clean, vet clean. **IMPORTANT: the Go toolchain IS present (`/opt/homebrew/bin/go` 1.26.4) — coding-3's "Go absent" was a false alarm; all Go tasks can verify.** Unblocks #22.
- [x] [coding] Persona CFN renderer | `source/claude_code_with_bedrock/persona_template.py` | `render_personas_stack(...)` emits valid CFN: per-persona Role (groups trust cond `ForAnyValue:StringEquals <issuer>:groups`), ManagedPolicy with Allow + **Deny across foundation-model/* , inference-profile/* , application-inference-profile/* **, permission boundary for restricted, `${AWS::Partition}`, `aws:RequestedRegion`, namespaced PutMetricData, `Output {Name}RoleArn`; pure fn. Run: `poetry run pytest tests/test_persona_template.py -q`
  > Done (coding-3, task-store #10). Pure `render_personas_stack(personas, groups_claim_name, issuer_host)` builds the template as a dict with **full-form** intrinsics (`Fn::Sub`/`Fn::ImportValue`/`Fn::GetAtt`/`Ref`) + `yaml.safe_dump` → parses with plain `safe_load`. Per persona: groups-gated Role (Federated = `Fn::ImportValue ${AuthStackName}-OIDCProviderArn`; `ForAnyValue:StringEquals {<issuer>:<claim>: [group]}`; AssumeRoleWithWebIdentity + TagSession; MaxSessionDuration 43200), access ManagedPolicy (region-scoped Allow via `aws:RequestedRegion`∈`AllowedBedrockRegions`, **Deny across all 3 ARN shapes** for restricted personas, List/Get, namespace-scoped PutMetricData), permission-boundary ManagedPolicy for restricted (also all-3-shapes Deny so it truly caps), exported `{Stem}RoleArn` Output (`${AWS::StackName}-{Stem}-RoleArn`). `${AWS::Partition}` on every ARN; logical-id sanitization + collision guard; raises on empty list / missing name|group. 17 unit tests pass; **rendered output is `cfn-lint` EXIT=0 clean** (dev-caught + fixed: E1029 bare-`${AWS::Partition}` outside Fn::Sub, and E6003 Output/Description-must-be-string). Consumed by deploy.py #15, the committed fixture #21, and bypass-guard #24. Verified via `.poetry-bootstrap/bin` poetry (coding-1's bootstrap) running the exact `Run:` command. ⚠️ REVIEWER NOTE: invoke actions are `InvokeModel`/`InvokeModelWithResponseStream`/`CallWithBearerToken` (the shipped `bedrock-auth-*.yaml` set) — NOT `Converse`/`ConverseStream` as the task text suggested. Converse is authorized under `InvokeModel` at runtime (no distinct IAM action) and listing it trips cfn-lint W3037 against the stale action DB — so this is functionally identical, lint-clean, and repo-consistent. Flag if Converse must be listed explicitly.
- [x] [coding] Budgets CFN renderer | `source/claude_code_with_bedrock/budgets_template.py` | `render_budgets_stack(...)` emits per-persona (cost-tag CostFilter) + account `AWS::Budgets::Budget`, 50/80/100% actual+forecast → `${AWS::StackName}-budget-alerts` topic w/ `aws:SourceAccount` TopicPolicy. Run: `poetry run pytest tests/test_budgets_template.py -q`
  > Done (coding-2, task-store #11). `render_budgets_stack(personas, account_budget_amount_usd=None) -> str` — pure fn, no IO. Emits: dedicated `AWS::SNS::Topic` `${AWS::StackName}-budget-alerts` (D7, separate from quota topic) + `AWS::SNS::TopicPolicy` granting `budgets.amazonaws.com` `sns:Publish` gated by `aws:SourceAccount == ${AWS::AccountId}` (confused-deputy guard, spec §5); one `AWS::Budgets::Budget` (COST/MONTHLY) per persona that has `budget_amount_usd` set, CostFilters `user:<TagKey>$<Value>` from `cost_tags` (sorted, OR-combined; single identifying tag recommended); optional account-total budget (no filter); every budget fires ACTUAL @50/80/100% + FORECASTED @100% → topic; `Output BudgetAlertsTopicArn` exported `${AWS::StackName}-BudgetAlertsTopicArn`. Intrinsics emitted full-form (`Fn::Sub`/`Ref`, no `!` tags) and anchor-free so output is cfn-lint-clean and human-reviewable. Raises `ValueError` on budgeted-persona-without-cost_tags and logical-id collisions. 18 unit tests pass; rendered reference output (eng 500 + sales 50 + account 1000) is **cfn-lint EXIT=0**; ruff lint+format clean. Consumed by deploy.py #15. NOTE: ran via `.venv/bin/python -m pytest` (poetry not installed on this host; the venv IS the poetry env — equivalent).
- [x] [coding] Wire persona→role selection in credential-process | `source/go/cmd/credential-process/main.go` | in `getAWSCredentials`, direct mode + non-empty Personas → resolve via `persona.Resolve(claims.GetStringSlice(groupsClaim), ...)` and assume `persona.RoleARN`; no-match+no-fallback → clear non-zero error; empty personas → unchanged `FederatedRoleARN`; all 3 call sites covered; no boto3. Run: `cd source/go && go test ./cmd/credential-process/ -count=1 && go build ./...`
  > Done (coding-1). Factored role selection into pure testable helper `selectRoleARN(cfg *config.ProfileConfig, claims jwt.Claims) (string, error)`; `getAWSCredentials` direct branch now calls it and feeds the result to the existing `federation.AssumeRoleWithWebIdentity`. Logic: empty `Personas` → `cfg.FederatedRoleARN` (BYTE-IDENTICAL to before — backward compat); else resolve groups via `claims.GetStringSlice(cfg.GroupsClaimName||"groups")` → `persona.Resolve(...)` → assume `p.RoleARN`; no-match+no-fallback → clear non-zero error ("no persona matched your groups …; contact your administrator"); matched-but-empty-RoleARN → clear error ("re-run ccwb package"). All 3 callers (run/trySilentRefresh/tryRefreshToken) route through getAWSCredentials so all covered. NO AWS SDK/boto3 added (credential-recursion.md — resolution is pure in-memory; only the pre-existing STS call). `buildSessionName` (sts.go) untouched — federation parity tests still green. 12 table cases in new main_test.go (match/no-match/fallback/match-beats-fallback/declared-order/custom-claim/scalar-claim/missing-claim/empty-RoleARN/empty-personas-backcompat) + default-claim test. `go build ./...` OK, `go test ./cmd/credential-process/` ok, `go vet ./...` clean, gofmt clean. Unblocks #25.
- [x] [coding] Persona validation helper | `source/claude_code_with_bedrock/persona_validation.py` | `validate_personas(personas, fallback)` → list of errors: dup names, empty/`group` missing, bad `enforcement_mode`, fallback not a persona, model glob sanity. Run: `poetry run pytest tests/test_persona_validation.py -q`
  > Done (coding-1, as task-store #13 which bundles validation + defaults). `validate_personas(personas, fallback) -> list[str]`: flags non-dict entry, missing/empty name or group, duplicate names, invalid `enforcement_mode` (∉ {alert,block}), non-string `allowed_models`/`denied_models` entries (and non-list fields), and fallback naming an unknown persona. Errors accumulate. 21 tests, ruff clean. See also the reference-defaults line below (same task/file group).

> Done when: both resolvers pass shared fixtures (parity), renderers produce lint-clean YAML, helper assumes persona role. Barrier before Group 3.

---

## Group 3: Integration — deploy, package, init, otel, lambdas, templates (wide parallel)
Spec ref: `design.md#2.3–2.8`. Disjoint files; consume Group 2 modules.

- [x] [coding] otel-helper persona header | `source/go/internal/otel/extract.go`, `source/go/internal/otel/headers.go`, `source/go/internal/otel/cache.go` | add `Persona` to `UserInfo`, `"persona":"x-persona"` to `HeaderMapping`, populate via `persona.Resolve` from claims+config; empty → no header (FormatHeaders exclusion); bump `currentCacheSchemaVersion`. Run: `cd source/go && go test ./internal/otel/ -count=1`
  > Done (coding-1). extract.go: added `Persona` to `UserInfo` + new `ExtractUserInfoWithPersona(claims, tagKey, personas, groupsClaimName, fallback)` that calls existing `ExtractUserInfoWithTagKey` then sets `info.Persona = p.Name` via `persona.Resolve` (independent resolution from same groups claim+config, spec D4 — no cross-binary handshake; defensive `err==nil && p!=nil` so a resolve miss just omits the header, never crashes telemetry). Existing `ExtractUserInfo`/`ExtractUserInfoWithTagKey` left intact (backward compat). headers.go: `"persona":"x-persona"` in HeaderMapping + `"persona":info.Persona` in FormatHeaders attrs — empty Persona excluded by existing empty-value filter (tested). cache.go: bumped `currentCacheSchemaVersion` 2→3. **otel-attribution-chain.md honored**: x-user-email ALWAYS present (asserted in both FormatHeaders tests). ALSO wired call site `cmd/otel-helper/main.go` (NOT owned by any other task — verified) to load cfg once and pass personas/claim/fallback to the new extractor; no config → no x-persona (graceful). No import cycle. 11 new tests in persona_header_test.go. `go test ./internal/otel/` ok, `go build ./...` ok, vet clean; my 5 files gofmt-clean (3 pre-existing test files have unrelated gofmt drift I did NOT touch). Unblocks #25.
- [x] [coding] deploy.py persona+budgets orchestration | `source/claude_code_with_bedrock/cli/commands/deploy.py` | add `persona`/`budgets` stack types; `_deploy_persona_stack`: FederationType pre-check (skip+warn on cognito), import OIDCProviderArn (fail clear if absent), render→build dir (utf-8)→deploy, seed GROUP quota policy per persona (reuse `QuotaPolicyManager`), create tagged inference profiles (idempotent check-then-create), deploy budgets + persona dashboard; gate on `effective_auth_type=="oidc"` + non-empty personas. Run: `poetry run pytest tests/test_deploy_personas.py -q && poetry run ruff check claude_code_with_bedrock/cli/commands/deploy.py`
  > Done (coding-3, task-store #15). Added `persona` + `budgets` stack types (single-`--stack` branches + all-stacks scheduling after quota, before codebuild), gated on `effective_auth_type=="oidc"` + non-empty `personas`. New methods: `_deploy_persona_stack` (reads `<auth>-FederationType` → cognito skips+warns per D5; reads `<auth>-OIDCProviderArn` → clear stack-ordering error if absent; renders via `render_personas_stack` → `build/personas/bedrock-personas.yaml` utf-8 → deploys with CAPABILITY_NAMED_IAM passing `AuthStackName`+`AllowedBedrockRegions`); `_seed_persona_group_policies` (one `PolicyType.GROUP` quota policy per persona, identifier=`group`, limits/mode from persona, tolerates PolicyAlreadyExists, skips cleanly if no policies table — D6); `_create_persona_inference_profiles` (idempotent check-then-create tagged AIPs, never fails the deploy); `_deploy_budgets_stack` (renders `render_budgets_stack` → deploys, skips when no per-persona/account budget); `_resolve_issuer_host` (scheme/slash-stripped issuer for the trust key, reuses `_resolve_oidc_config`). **PERSONA_ORDER wiring (step 6)**: new `_compute_persona_order` → comma-joined persona `group`s in DECLARED order (de-duped), passed as `PersonaOrder=` CFN param on the QUOTA stack (param name confirmed from devops #19's quota-monitoring.yaml); empty when no personas / not-oidc → Lambdas stay legacy most-restrictive (D3). **role_arn write-back (step 8)**: `_write_back_persona_role_arns` reads `{Stem}RoleArn` outputs (reuses renderer's `_logical_id` so keys can't drift) into each persona dict AND persists via `Config.load().save_profile(profile)` so the separate `ccwb package` invocation sees them (get_profile returns a fresh Profile — in-memory mutation alone would be lost). 25 tests (scheduling gates, cognito-skip, missing-OIDC-error, happy-path deploy assertions, declared-order PERSONA_ORDER incl. dedup, role_arn write-back+persist+sanitized-key, budgets skip/deploy). `Run` PASSES (pytest + ruff clean — also fixed 5 PRE-EXISTING ruff violations in this owned file so the gate passes, behavior-identical, per coding-1's #4 precedent). Verified with `.poetry-bootstrap` poetry. ⚠️ STEP 9 NOT DONE: deploying `bedrock-personas-dashboard.yaml` is left unwired — it needs its own stack-type branch and #20's template only just landed; not in this task's `Run` gate. Flagged to lead as a small follow-up.
- [x] [coding] package.py persona serialization | `source/claude_code_with_bedrock/cli/commands/package.py` | `_create_config` emits `personas[]` (with resolved `role_arn` from stack outputs), `groups_claim_name`, `fallback_persona`; only when personas configured; encoding utf-8. Run: `poetry run pytest tests/test_package_personas.py -q`
  > Done (coding-1, task-store #16). After the quota block in `_create_config`, when `profile.personas` is non-empty: emit `personas` (each projected by new `_serialize_persona` static method to EXACTLY the §4.2 Go-consumed fields — name/group/role_arn always present; display_name/allowed_models/denied_models/monthly_token_limit/enforcement_mode/cost_tags only when truthy, matching Go `omitempty`), plus `groups_claim_name`, and `fallback_persona` only when set. Python-only fields (`daily_token_limit`, `budget_amount_usd`) deliberately NOT serialized — the Go struct has no tags for them. **role_arn contract**: each persona dict carries its own `role_arn` key (default assumption from the task); deploy.py #15 must write it back into the persona dicts from stack outputs — flagged to coding-2 (owns #15). Empty/unset → emitted as `""` so the field is always present for the helper. No personas configured → no persona keys at all (legacy `federated_role_arn` path untouched). 8 tests (serialization, §4.2 projection, omitempty, required-always-present, backward-compat). Reused existing utf-8 write. Test file ruff-clean; my added package.py lines ruff-clean (the file's 28 other E501/B007 are PRE-EXISTING, not gated by this task's Run).
- [x] [coding] init.py persona wizard | `source/claude_code_with_bedrock/cli/commands/init.py` | after quota section, SSO+quota-gated questionary loop collects personas + `groups_claim_name` + `fallback_persona`; `wizard_fields` includes them in `_save_configuration`. Run: `poetry run pytest tests/test_init_personas.py -q`
  > Done (coding-2, task-store #17). Added a persona section right before `monitoring_complete` save, gated on `config["sso_enabled"]` AND `config["quota"]["enabled"]` (quota-requires-oidc.md). Factored two seams per the task's design note: `_persona_from_wizard_answers(answers)` (pure: CSV→list model globs, optional int limits / float budget→None on blank, `Key=Value,…`→cost_tags dict, display_name defaults to name) and `_gather_personas(config)` (interactive: opt-in → seed from `REFERENCE_PERSONAS` [deep-copied so the constant isn't mutated] or empty → custom-persona loop [blank name ends] → `groups_claim_name` + `fallback_persona` → `validate_personas` with recurse-on-error/decline-to-skip). Added module-level `_is_positive_number` helper. Wired `personas`/`groups_claim_name`/`fallback_persona` into `wizard_fields` in `_save_configuration` (defaults []/"groups"/None for non-persona profiles → backward-compat). 18 new tests (pure-transform parsing + questionary-patched loop happy/decline/reentry paths + Profile mapping), all pass; existing `tests/cli/commands/test_init.py` still green (52 combined). Verified via `.poetry-bootstrap/bin` poetry running the exact `Run:` command. NOTE: `init.py` had ~14 PRE-EXISTING ruff E501 long lines unrelated to this task (this task's Run is pytest-only, no ruff gate); I introduced ZERO new E501 (fixed my one long lambda) and did NOT run `ruff format` on the file because it would reformat dozens of pre-existing lines outside my scope — flag for a separate formatting pass if desired.
- [x] [devops] Lambda declared-order resolution (PBAC mode) | `deployment/infrastructure/lambda-functions/quota_check/index.py`, `deployment/infrastructure/lambda-functions/quota_monitor/index.py` | gated on new `PERSONA_ORDER` env var: first-declared-group wins; unset → legacy most-restrictive `min()` preserved; `extract_groups_from_claims` unchanged. Run: `poetry run pytest ../deployment/infrastructure/lambda-functions/ -q` (or module-local unittest)
  > Done (devops-1, task-store #18). Added module-level `PERSONA_ORDER = [g for g in os.environ.get("PERSONA_ORDER","").split(",") if g.strip()]` to BOTH Lambdas. `resolve_quota_for_user` (quota_check) and `resolve_user_quota` (quota_monitor): restructured the group tier to map enabled group policies by group value, then — when `PERSONA_ORDER` is set (PBAC mode, D3) — return the first declared group's policy (mirrors helper §4.3); when unset/empty, the legacy `min()` most-restrictive branch is preserved byte-for-byte. **PBAC subtlety**: a user whose only group policies are NOT in `PERSONA_ORDER` falls through to the default tier (declared order is the sole group authority — does NOT silently revert to most-restrictive). `extract_groups_from_claims` untouched (still returns a set). User>group>default precedence unchanged. New `tests/test_lambda_persona_order.py`: 17 tests (per-Lambda first-declared-wins / reversed-order / legacy-unset / legacy-empty-string / single-group / no-declared-group→default / user>group precedence) + a cross-Lambda parity check that both resolvers agree. **17 passed; existing quota/Lambda suites 63 passed (no legacy regression)**; changed Lambda files + test ruff-clean. Ran via `.venv/bin/python -m pytest` (poetry not on PATH; the in-project `.venv` IS the poetry env — equivalent). NOTE: `PERSONA_ORDER` is a new Lambda env var that the Group-5 `PBAC_README.md` should document under config/enforcement; deploy.py #15 must emit it (comma-joined persona group values in declared order) onto both functions and quota-monitoring.yaml #19 wires the empty default.
- [x] [devops] quota-monitoring.yaml PERSONA_ORDER wiring | `deployment/infrastructure/quota-monitoring.yaml` | add `PERSONA_ORDER` env var (empty default) to quota_check + quota_monitor functions; cfn-lint clean. Run: `cd .. && poetry run cfn-lint deployment/infrastructure/quota-monitoring.yaml`
  > Done (devops-2, task-store #19). Added CFN Parameter `PersonaOrder` (Type String, Default `''`) with a Description documenting PBAC declared-order semantics + who populates it (`ccwb deploy`). Wired `PERSONA_ORDER: !Ref PersonaOrder` onto BOTH `QuotaMonitorFunction` and `QuotaCheckFunction` Environment.Variables — empty default = legacy most-restrictive, matching devops-1's #18 contract note ("quota-monitoring.yaml #19 wires the empty default"); gives #15 deploy.py the param to pass the comma-joined declared order. cfn-lint EXIT=4 = the unchanged HEAD baseline (3 pre-existing W3002 `Code:`-path warnings on the local `./lambda-functions/*` paths — intentional for CFN packaging); **zero new findings** — verified by diffing cfn-lint output against `git show HEAD:deployment/infrastructure/quota-monitoring.yaml`. Docs synced (documentation skill): added a `PersonaOrder` row to the Configuration Settings table in `assets/docs/QUOTA_MONITORING.md` and a concise "PBAC override" note under Policy Precedence (declared-order overrides most-restrictive when set) with a forward pointer to PBAC docs; all three doc claims verified against the template. Ran cfn-lint via `source/.venv/bin/cfn-lint` (poetry not on PATH on this host; the in-project venv IS the poetry env — equivalent).
- [x] [devops] Persona dashboard + Logs Insights queries | `deployment/infrastructure/bedrock-personas-dashboard.yaml`, `deployment/infrastructure/logs-insights-queries.yaml` | separate dashboard (cfn-naming compliant) with per-persona token/spend/Allow-Deny widgets + top-user leaderboard (group by `persona`); append per-persona QueryDefinitions. Run: `cd .. && poetry run cfn-lint deployment/infrastructure/bedrock-personas-dashboard.yaml deployment/infrastructure/logs-insights-queries.yaml`
  > Done (devops-1, task-store #20). NEW `bedrock-personas-dashboard.yaml` — separate `AWS::CloudWatch::Dashboard` (param `DashboardName` default `ClaudeCodePersonasDashboard`, cfn-naming compliant `!Ref`; `MetricsRegion` param), mirrors the existing dashboard's native-PromQL widget JSON exactly. 9 chart widgets grouping `claude_code.token.usage`/`claude_code.cost.usage` by the `persona` label: Total Tokens by Persona (pie), Cost by Persona (pie), Token Usage by Persona over time, Daily Tokens by Persona (`increase(...[1d])`), Tokens by Persona+type, Active Users by Persona, Cost by Persona over time, and the top-user leaderboard (Top 20 by tokens AND by cost, grouped `("user.email", persona)`). DashboardBody embedded JSON validated parseable (13 widgets, all charts retain plotOptions). Appended 4 `AWS::Logs::QueryDefinition`s to `logs-insights-queries.yaml` (Token Usage by Persona, …Over Time Daily, Cost by Persona, Top Users by Persona) parsing `"persona":"..."` with `coalesce(persona,"(none)")` so unmatched users stay visible; updated the QueryListInfo output. **cfn-lint EXIT=0 on BOTH files** (dashboard clean; the long-standing pre-existing W2001 `MetricsLogGroup not used` — present on HEAD — is now also resolved as a clean side effect because the 4 new queries `!Ref MetricsLogGroup`; existing 16 queries untouched). Ran via `source/.venv/bin/cfn-lint` (poetry not on PATH; venv IS the poetry env). ⚠️ DEPENDENCY: the `persona` metric label only populates once the OTLP collector maps `x-persona`→`persona`. `otel-helper` #14 emits the `x-persona` HEADER, but `otel-collector.yaml` needs the matching `from_context: metadata.x-persona`→`key: persona` attribute action AND a `dimensions: [[persona, OTelLib]]` metric_declaration — that file is NOT in any current task's scope. Flagged to team-lead as a gap. Dashboard/queries are correct regardless; they render empty until that wiring lands. Group-5 PBAC_README dashboards section should reference the dashboard name + this dependency.
- [x] [devops] Committed persona stack fixture for CI | `deployment/infrastructure/bedrock-personas.example.yaml` | rendered from the 2 reference personas (eng+sales); the bypass-proof Deny on all 3 ARN shapes visible; cfn-lint + cfn_nag clean. Run: `cd .. && poetry run cfn-lint deployment/infrastructure/bedrock-personas.example.yaml`
  > Done (devops-2, task-store #21). Rendered the NEW committed fixture by running `render_personas_stack` (#10) over `REFERENCE_PERSONAS` (#13, engineering+sales) with `groups_claim_name="groups"`, sample `issuer_host="auth.example.com"`, written utf-8 with a "DO NOT EDIT — regenerate" header documenting source + the bypass-guard invariant. The sales persona's `DenyBedrockInvokeDeniedModels` covers ALL 3 ARN shapes for sonnet AND opus (`foundation-model/*`, `inference-profile/*anthropic.*{sonnet,opus}*`, `application-inference-profile/*anthropic.*{sonnet,opus}*`) — the bypass-proof exemplar (spec D8 / R-highest), plus a matching permission boundary. **cfn-lint EXIT=0, fully clean** (brand-new file, no pre-existing warnings). Verified it self-enrolls into CI: both the pre-commit cfn-lint hook (`files: deployment/infrastructure/.*\.(yaml|yml)$`) and CI `cfn-lint deployment/infrastructure/*.yaml` (pytest-ci.yml) glob-match it automatically — no list to update; CI additionally `--ignore-checks` 11 codes (W3002 et al.), all of which my clean run already satisfies. cfn_nag not installed on this host (not in the `Run:` command; policy structure mirrors the cfn_nag-clean auth templates). Used a throwaway render script (removed after; not committed). Ran cfn-lint via `source/.venv/bin/cfn-lint`. NOTE: regenerate this fixture whenever `persona_template.py` or `REFERENCE_PERSONAS` changes.
- [x] [coding] Register reference personas in init defaults + sample config | `source/claude_code_with_bedrock/cli/commands/init.py` is OWNED by the wizard task above — instead put seed data in `source/claude_code_with_bedrock/persona_defaults.py` | `REFERENCE_PERSONAS` constant (engineering, sales) per design §3 importable by wizard + tests. Run: `poetry run pytest tests/test_persona_defaults.py -q`
  > Done (coding-1, folded into task-store #13). `REFERENCE_PERSONAS = [engineering, sales]` in `persona_defaults.py`, canonical §4.1 shape (display_name, allowed/denied globs, monthly_token_limit, daily_token_limit=None, enforcement_mode=block, budget_amount_usd=None, cost_tags). engineering: eng-team, allowed `["anthropic.*"]`. sales: sales-team, allowed `["anthropic.*haiku*"]`, denied sonnet+opus globs. Tests assert both validate cleanly. NOTE: defaults tests live in `tests/test_persona_validation.py` (#13's owned test file), not a separate `test_persona_defaults.py` — the task store merged the two tasks into #13.

- [x] [devops] Add persona dimension to OTEL collector (integration gap, found by devops-1) | `deployment/infrastructure/otel-collector.yaml` | `x-persona`→`persona` upsert in BOTH from_context blocks (~382, ~496) + `[[persona, OTelLib]]` in metric_declarations (~430); cfn-lint clean vs HEAD baseline. Run: `cd source && poetry run cfn-lint ../deployment/infrastructure/otel-collector.yaml`
  > Done (devops-1, task-store #26). Closed the gap I found in #20. The `OTelConfig` SSM param holds TWO embedded collector configs via `Value: !If AnalyticsEnabled`: (A) the EMF+OTLP block (has `awsemf` exporter + `metric_declarations`) and (B) the OTLP-only block (no awsemf). Added the persona `attributes` action `- key: persona / from_context: metadata.x-persona / action: upsert` to BOTH blocks' `attributes.actions`, placed right after `team.id` (declared-order neighbor, mirrors the x-department/x-team-id pattern). Added `- dimensions: [[persona, OTelLib]]` to the EMF block's `metric_declarations` only (block B has no awsemf — correct; OTLP export carries the attribute natively). Verified: **cfn-lint EXIT=0 = HEAD baseline (also 0), zero new findings**; AND extracted+`yaml.safe_load`-parsed BOTH embedded configs to confirm they stay well-formed after the in-`!Sub` edits (block A: persona attr+dimension present, order team.id→persona→cost_center; block B: persona attr present, no dimension as expected). Respects otel-attribution-chain.md: persona is `upsert` from context, so when the header is absent (unmatched user → otel-helper omits x-persona, #14) the attribute simply isn't added for that datapoint — no empty dimension emitted. This is the plumbing that makes #20's persona dashboard + Logs Insights queries actually populate. Ran cfn-lint via `source/.venv/bin/cfn-lint` (poetry not on PATH; venv IS the poetry env). Unblocks #25.

> Done when: deploy renders+deploys persona/budgets/dashboard, package serializes personas, wizard collects them, otel emits x-persona, **collector maps x-persona→persona dimension (#26)**, lambdas honor declared order, fixtures lint clean. Barrier before Group 4.

---

## Group 4: End-to-end validation + cross-impl parity
Spec ref: `spec.md#4.3` parity, FR-9.4. Depends on Groups 2–3.

- [x] [coding] Go↔Python persona-resolution parity test | `source/tests/test_persona_parity.py` | drive Go resolver (via compiled binary or `go run` harness) and Python resolver over the SAME Group-1 fixtures; assert identical persona names; include the buildSessionName-unchanged assertion. Run: `poetry run pytest tests/test_persona_parity.py -q`
  > Done (coding-2, task-store #22). 17 tests. Python side: `resolve_persona` run over every shared fixture (#7) asserting it yields each case's `expected` (aggregate + parametrized-per-case). Go side: shells out to `go test ./internal/persona/ -run TestResolveAgainstSharedFixtures` (coding-1's #9 test that loads the SAME fixture file) — exit 0 ⇒ Go agrees with the fixtures. Transitive parity made explicit: both impls pinned to one `expected` oracle per case ⇒ Python==Go for all cases. buildSessionName guard: shells `go test ./internal/federation/ -run SessionName` and requires exit 0 (credential-helper-parity.md #204 — persona work must not touch it). Go discovery via `shutil.which` + Homebrew/standard-path fallback; Go-dependent tests FAIL (not skip) if the toolchain is missing so CI never silently passes on drift. EXIT=0 (17 passed), ruff clean. **Note: Go IS installed (`/opt/homebrew/bin/go` 1.26.4, added mid-session); my earlier "Go absent" blocker reports were stale — disregard.** Verified via `.poetry-bootstrap/bin` poetry + `/opt/homebrew/bin` on PATH. Unblocks #25.
- [x] [coding] Backward-compat regression | `source/tests/test_backward_compat_personas.py` | pre-persona profile/config.json loads; `personas==[]`; helper path uses `FederatedRoleARN`; legacy multi-group quota = most-restrictive (PERSONA_ORDER unset). Run: `poetry run pytest tests/test_backward_compat_personas.py -q`
  > Done (coding-2, task-store #23). 9 tests across the 3 layers PBAC touched: (1) Config — a pre-persona profile dict (no persona keys) loads via `Profile.from_dict` with `personas==[]`/`groups_claim_name=="groups"`/`fallback_persona is None`, `federated_role_arn` preserved, `effective_auth_type` correct for legacy `sso_enabled` True/False/missing, and `to_dict` invents no phantom personas. (2) Packaging — `PackageCommand._create_config` for a non-persona profile writes a `config.json` with NO `personas`/`groups_claim_name`/`fallback_persona` keys (historical shape intact, helper falls back to `FederatedRoleARN`). (3) Quota — loads `quota_check/index.py` fresh with `PERSONA_ORDER` unset (asserts `mod.PERSONA_ORDER == []`) and `ENABLE_FINEGRAINED_QUOTAS=true`, mocks `get_policy`, confirms a multi-group user resolves to the MOST-restrictive (lowest-limit) group policy regardless of group order — legacy D3 path preserved. Reuses #18's fresh-import lambda pattern (the exhaustive PBAC-vs-legacy matrix stays in #18; this asserts only the legacy-preserved direction). EXIT=0, ruff clean. Verified via `.poetry-bootstrap/bin` poetry. Blocks #25.
- [x] [coding] Inference-profile bypass guard test (R-highest) | `source/tests/test_persona_policy_bypass.py` | assert rendered sales policy Denies `inference-profile/*sonnet*` and `application-inference-profile/*` and `foundation-model/*sonnet*` (all 3); a foundation-model-only Deny must fail the test. Run: `poetry run pytest tests/test_persona_policy_bypass.py -q`
  > Done (coding-1, task-store #24). Renders the **sales** REFERENCE_PERSONA (#13) via coding-2's `render_personas_stack` (#10), parses the YAML, extracts every Deny statement's `Fn::Sub` ARNs, and asserts each denied keyword (sonnet, opus) is covered on ALL 3 shapes — `:foundation-model/`, `:inference-profile/`, `:application-inference-profile/` (shape match anchored on leading `:` so inference-profile ≠ application-inference-profile). Separately asserts the access-policy `DenyBedrockInvokeDeniedModels` statement is self-sufficient (not relying on the boundary). Includes the required meta-test: a foundation-model-only Deny fixture is asserted to be flagged as missing the 2 profile shapes — i.e. the guard provably has teeth. Plus engineering(no denied_models)→no Deny. 5 tests pass, ruff clean. Unblocks #25.
- [x] [coding] Fix cmd/otel-helper cache fixtures after schema bump 2→3 (task-store #28) | `source/go/cmd/otel-helper/main_test.go` | 4 failing cache tests pass; x-user-email preserved. Run: `cd source/go && go test ./cmd/otel-helper/ -count=1`
  > Done (coding-1). Surfaced by the #25 full-suite gate: my #14 cache-schema bump (2→3) made 4 otel-helper tests that seed `"schema_version":2` fixtures fail — ReadCachedHeaders now treats v2 as stale and refreshes instead of serving (the bump's intended effect). Bumped the 4 fixture literals 2→3 (intent-preserving — these tests assert Layer-1 cache-serving / no-clobber behavior, not a specific version). `go test ./cmd/otel-helper/` green; x-user-email assertions intact (14 refs). File was owned by no task; lead created #28 for it concurrently with my fix — claimed + closed.
- [x] [coding] Full Python suite + Go suite green | (no new files; runs existing) | `[skip-format-check]` aggregate gate. Run: `poetry run pytest tests/ -q && cd go && go test ./... -count=1`
  > Done (coding-1, task-store #25). Final integrated run: **Python 1118 passed / 0 failed** (22 skipped, 1 xfailed, 1 xpassed); **Go all packages ok**. This gate caught 2 real integration failures which were fixed before closing: (1) destroy.py teardown gap for budgets+persona → task-store #27 (its owner); (2) my own #14 cache-schema bump broke 4 otel-helper fixture tests → task-store #28 (fixed by me). After both landed, both suites are clean. Whole persona feature integrates end-to-end. Barrier before review cleared.

> Done when: parity holds, backward-compat proven, bypass impossible, both suites green. Barrier before review.

---

## Review (Phase 2 step 12-13) + Fix cycle (Phase 3) — COMPLETE ✅
Consolidated verdict: **PASS** (all 4 scopes). See `review-summary.md` + the 4 `review-<scope>.md`.
- go-helper (review-2): PASS · infra-lambda (review-3): PASS · tests-parity (review-4): PASS · python-cli (review-1): FAIL→PASS after fix cycle.
- **Fix cycle (3 tasks, all closed):** #30 [coding] CRITICAL issuer-host Auth0/Azure slash (deploy.py) · #31 [coding] account_budget_amount_usd wiring (config.py/init.py) · #32 [coding] persona-dashboard teardown FR-9.5 (destroy.py). Lead authoritative re-gate: **Python 1128/0, Go 10/10 ok.**
- These caught 3 real defects 1120 green tests missed (silent Auth0 lockout, unreachable account budget, orphaned dashboard stack) — the review gate's core value.

## Group 5: Documentation (FR-10) — COMPLETE ✅
Spec ref: FR-10. Authored via `documentation` skill in Phase 4 (lead-coordinated).

- [x] [devops] PBAC_README.md (#33, devops-1) | `PBAC_README.md` (repo root) | covers FR-10.2 (a)-(m): overview, config schema, per-IdP groups setup, deploy flow + stack ordering + Cognito skip, the inference-profile Deny invariant as a prominent ⚠️, enforcement (OTEL+quota reuse), cost attribution, budgets+SNS, dashboards, dynamic admin + rollback, no-match/fallback, auth-type/Cognito limits, troubleshooting. Reflects ACTUAL behavior + Auth0/Azure issuer-host ⚠️. Run: `test -f PBAC_README.md && grep -qi persona PBAC_README.md && npx --yes markdownlint-cli2 PBAC_README.md || true`
  > Done (devops-1, task-store #33). New `PBAC_README.md` (repo root), 13 sections covering FR-10.2 (a)-(m). Two prominent ⚠️ warnings: §3 issuer-host (Auth0 keeps trailing slash `company.auth0.com/`, Azure `.../v2.0`, scheme-strip-only — the #30 fix; getting it wrong = silent hard-deny, with a matching §13 troubleshooting row) and §5 the 3-ARN inference-profile Deny invariant (D8/FR-2.3). Tables throughout (scannable per task), links to spec.md/decisions.md/rules rather than duplicating. Drew detail from spec + decisions.md + verified against shipped code. **Accuracy pass via `pr-review-toolkit:comment-analyzer`** (worked this time — no off-topic refusal): it verified all 6 high-risk claims accurate (issuer-host scheme-strip, 3-ARN Deny, PERSONA_ORDER sole-authority+default-tier fallthrough, fallback hard-deny, Cognito/IDC/Google limits, reference-persona values) and found **2 real doc-vs-code inaccuracies, both FIXED**: (HIGH) the bypass assertion is the pytest `test_persona_policy_bypass.py`, NOT the `ccwb test` command — I'd echoed spec.md:131's aspirational text; corrected §4/§5/§13 (esp. the §13 "re-run ccwb test" false-reassurance row → now "inspect the rendered ManagedPolicy / guarded by the pytest"); (LOW) §4 deploy-flow said the persona dashboard deploys "after budgets" but it's inline at the tail of the persona-stack step BEFORE budgets (deploy.py:285-287,1446) — corrected. markdownlint is advisory (`|| true`); findings are default-ruleset MD013/MD060 noise — the repo has NO markdownlint config and the shipped README.md trips 121 of the same, so PBAC_README is consistent with repo style. Run passes (exists + content). Unblocks #34. **Post-completion fix (lead-caught, verified against code):** §3 trust-condition table Okta row was WRONG — I'd put issuer-host `company.okta.com/oauth2/default` (the ALB token-VALIDATION issuer from `_resolve_oidc_config`); the persona trust-condition issuer-host is the **bare domain `company.okta.com`** (the IAM OIDC provider is registered `Url: https://${OktaDomain}` — bedrock-auth-okta.yaml:68; `_resolve_issuer_host` docstring deploy.py:1329 states this explicitly; test_persona_template/test_persona_policy_bypass both use `ISSUER_HOST = "company.okta.com"`). Same high-stakes class as #30 — corrected the row to bare domain (no path, no slash). Auth0 slash + Azure /v2.0 rows were already correct. Re-verified Run passes.
- [x] [devops] Link PBAC_README from main README (#34, devops-1, blocked by #33) | `README.md` | features/docs section links to `PBAC_README.md`. Run: `grep -q "PBAC_README.md" README.md`
  > Done (devops-1, task-store #34). Added one link line in `README.md` "Additional Resources → Architecture & Deployment" (the canonical docs index, README.md:427): "Persona-Based Access Control & Cost Governance (PBAC_README.md) — Optional layer mapping OIDC groups to model-access tiers, with per-persona token quotas, cost-allocation tags, and budgets." Minimal one-line diff, no restructure (pr-standards.md); framed as an OPTIONAL layer on the existing ccwb deployment per the task. Run passes (`grep -q PBAC_README.md README.md`).

## Post-build: independent deep-dive review + Low-issue wave (2026-06-16) — COMPLETE ✅
After team teardown, the user requested an independent deep-dive of the whole `rubab-dev1` diff (3 parallel skeptical reviewers + lead reads of high-risk surfaces), then a decided Low-issue wave. Full detail in `decisions.md`.

**Deep-dive fixes (found beyond the team's own passing review):**
- [x] [coding] **H1 (HIGH)** — `_resolve_issuer_host` used `provider_domain` for generic OIDC; must use `oidc_issuer_url` (the registered provider URL, often a realm path) → was silently hard-denying ALL generic/**Teleport** persona users. Same class as the Auth0 CRITICAL. Fixed + Auth0/Azure/**generic** regression tests. `deploy.py`, `test_deploy_personas.py`.
- [x] [coding] **M1 (MEDIUM)** — `validate_personas` ran only in the wizard; now also at the top of `_deploy_persona_stack` so a hand-edited bad `enforcement_mode` fails loudly instead of silently downgrading block→alert. + regression test.
- [x] [coding] **M2 (MEDIUM)** — removing a persona orphaned its inference profile / left a stale GROUP quota policy. Deploy now DETECTS orphaned `{pool}-*` AIPs and prints the manual delete command (chose warn-not-auto-delete: a deploy must not implicitly remove billing/quota resources). Documented in PBAC_README §10.

**Low-issue wave (user-decided per-item on new-in-branch vs pre-existing provenance):**
- [x] [coding] **L1** — dropped the dead `auth_type` passthrough in `effective_auth_type` (net-new in branch, unreachable). `config.py` + test rewritten.
- [—] **L2 — SKIPPED** (Cognito issuer-host pool-region): pre-existing on main AND unreachable via PBAC (direct-IAM only). Not ours.
- [x] [coding] **L3** — gate persona serialization in `package.py` on `federation_type == "direct"` (no dead persona data under Cognito). + regression test.
- [x] [devops] **L4** — `source/otel_helper/otel-helper.sh` sidecar shim now honors the cache `schema_version` gate (the branch's 2→3 bump was being undermined by the `.raw` fast-path). bash -n clean.
- [x] [coding] **L5** — per-persona ALERTING wired: `quota_check` persists `USER#<email>/GROUPS` (TTL 90d, scoped `PutItem` IAM); `quota_monitor` reads it via `get_user_groups` so its declared-order branch resolves persona alert thresholds. + 5 lambda tests. `quota_check/index.py`, `quota_monitor/index.py`, `quota-monitoring.yaml`.

**Wave re-gate (lead-run): Python 1136/0, Go 10/10, cfn-lint W3002 unchanged (3=3), config.py+deploy.py ruff-clean** (package.py 23 E501 pre-existing). Docs aligned: `PBAC_README.md` (§3 generic/Okta rows, §6 alerting, §10 orphan cleanup, §13 generic troubleshooting), `spec.md §0` amendments, `design.md` status banner.

## Post-build: THIRD independent deep-dive (2026-06-15) — COMPLETE ✅
User requested a further deep-dive after the FR-5.1 routing work (A8). 4 parallel skeptical agents (Lambda / Python wizard-destroy-package / docs-vs-code / test-gaps) + lead verification of every Tier-1 surface, each agent finding re-verified against code (caught 2 false positives). Findings all in the FR-5.1 wrapper install/teardown surface. Full detail in `decisions.md` ("Third independent deep-dive") + `spec.md §0 A10`.

- [x] [coding] **HIGH** — FR-5.1 launch wrapper generated but never installed: neither `install.sh` nor `install.bat` copied `persona-model.{sh,ps1}` to `$HOME/claude-code-with-bedrock/` (the path PBAC_README §7 tells users to source) → routing silently inert via the documented flow. Both installers now copy it + echo the source line. | `package.py`, `test_package_personas.py` | install.sh/bat contain the wrapper-copy; bash -n clean. Run: `cd source && .venv/bin/python -m pytest tests/test_package_personas.py -q`
- [x] [coding] **MED** — `--regenerate-installers` never re-emitted the wrapper. Now calls `_create_persona_model_wrapper`. | `package.py`, `test_package_personas.py` | `test_regenerate_installers_emits_wrapper` passes.
- [x] [coding] **MED** — wizard could save a budgeted persona with empty `cost_tags` that crashes `ccwb deploy`. `validate_personas` now rejects it (wizard + hand-edit). | `persona_validation.py`, `test_persona_validation.py` | teethed test passes.
- [x] [coding] **MED** — destroy derived AIP names from CURRENT `entitled_tiers`, orphaning AIPs for tiers a persona lost post-deploy. Teardown now sweeps ALL tiers + legacy; first REAL-method test (was only ever patched no-op). | `destroy.py`, `test_destroy_stacks.py` | `TestPersonaInferenceProfileTeardown` passes (teethed).
- [x] [coding] **TEST** — committed `bedrock-budgets.example.yaml` so CI cfn-lints the rendered Budgets stack (persona stack had this; budgets didn't) + drift guard; rewrote tautological `test_unresolvable_tier_returns_none`. | `deployment/infrastructure/bedrock-budgets.example.yaml`, `test_budgets_template.py`, `test_persona_models.py` | cfn-lint clean; drift guard passes.

**Third-pass re-gate (lead-run): Python 1207/0, Go 10 pkgs ok, cfn-lint clean (incl. new budgets fixture), zero new ruff** (package.py 20 E501 == HEAD). False positives (verified, no action): otel `ExtractUserInfoWithPersona` Go tests + `jwt.GetStringSlice` scalar/non-string cases already exist. Docs realigned: PBAC_README §2/§5/§7a + TOC/§13 anchors; spec.md §0 A10 + status; this file.

## Post-build: 3rd-pass LOW-wave + FR-5.1 integration coverage (2026-06-16) — COMPLETE ✅
User directed fixing ALL 5 reported LOWs and adding install→use→teardown integration coverage. File-disjoint, each teethed. Full detail in `decisions.md` (2026-06-16 LOW-wave entry) + `spec.md §0 A10` follow-on.

- [x] [coding] **L1** — extract `tokenExpired` Go helper; exit-code-4 path now tested. | `go/cmd/credential-process/main.go`, `main_test.go` | `TestTokenExpired` passes. Run: `cd source/go && go test ./cmd/credential-process/`
- [x] [coding] **L2** — PS1 wrapper PATH-resolves `claude` (`-CommandType Application`, anti-recursion) instead of hardcoding `claude.cmd`. | `package.py`, `test_package_personas.py` | `test_ps1_resolves_claude_on_path_not_hardcoded_cmd` passes.
- [x] [coding] **L3** — init validation-retry skips the opt-in confirm (`_retry` param). | `init.py`, `test_init_personas.py` | `test_retry_skips_optin_prompt` passes (opt-in asked once).
- [x] [coding] **L4** — `store_user_groups` write gated on `ENABLE_FINEGRAINED_QUOTAS` (monitor only reads it then). | `quota_check/index.py`, `test_lambda_persona_order.py` | `test_groups_not_written_when_finegrained_disabled` passes (teethed).
- [x] [coding] **L5** — no budget for a zero-entitled-tier persona. | `budgets_template.py`, `test_budgets_template.py` | `test_zero_entitled_tier_persona_gets_no_budget` passes (teethed).
- [x] [coding] **INTEGRATION** — FR-5.1 install→use→teardown: package → real Go helper on the packaged config.json → destroy, locking AIP-name/ARN consistency across both languages. | `tests/integration/test_persona_model_lifecycle.py`, `go/cmd/credential-process/main_test.go` (`TestPersonaModelExportsFromConfigJSON`) | 4 Python + 1 Go bridge pass; use-leg fails-not-skips without Go, teeth-verified.

**LOW-wave + integration re-gate: Python 1215/0, Go 10 pkgs ok, go vet clean, zero new ruff/E501.**

## Post-build: 4th independent deep-dive (2026-06-16) — COMPLETE ✅
6-scope adversarial pass; every finding re-verified against code (incl. AWS service-auth docs) before acting. Central question "did Hardening passes 1-3 introduce regressions?" → yes, one (global-CRIS inert). Full detail in `decisions.md` (2026-06-16 4th deep-dive entry) + `spec.md §0 A11`.

- [x] [coding] **MED** — global-CRIS Allow/Deny were INERT (the A9 fix built `foundation-model/anthropic.*<tier>*`, no leading `*`; real ids are `global.anthropic.…`, anchored-match never hit). Prepend `*` like the inference-profile shapes. | `persona_template.py`, `test_persona_template.py`, `bedrock-personas.example.yaml` | new `test_global_cris_allow_glob_actually_matches_real_global_model_ids` uses fnmatch against real global ids (teethed); fixture regenerated cfn-lint-clean. Run: `cd source && .venv/bin/python -m pytest tests/test_persona_template.py -q`
- [x] [coding] **MED** — persona-dashboard orphan false-positive: inline dashboard flagged "disabled in config" + offered for deletion on every all-stacks re-deploy with personas. Exclude it from orphan-eligibility while the persona stack is deploying. | `deploy.py`, `test_deploy_personas.py` | new `TestOrphanedStackCheck` (2 cases, teethed; `_check_orphaned_stacks` had zero coverage). Run: `cd source && .venv/bin/python -m pytest tests/test_deploy_personas.py -q`
- [x] [coding] **LOW (parity)** — Python `resolve_persona` scalar-groups divergence from Go `GetStringSlice` (wrap-not-iterate a `str`); reference half of §4.3 parity contract. | `persona_resolution.py`, `test_persona_resolution.py` | +2 parity tests (scalar match + scalar non-substring). Run: `cd source && .venv/bin/python -m pytest tests/test_persona_resolution.py tests/test_persona_parity.py -q`
- [x] [docs] **DOC** — PBAC_README §11 (Lambdas don't honor `fallback_persona` — fallback = model access, not quota tier), §6 (`ENABLE_FINEGRAINED_QUOTAS` in write gate), §3 (redundant `https://` in generic cell); spec §0 A11 + status (1220/0); decisions.md; this file. | `PBAC_README.md`, `spec.md`, `decisions.md`, `tasks.md` | docs match code; README.md confirmed already-minimal (single link).

**4th-pass re-gate: Python 1220/0 (+5), Go 10 pkgs ok, go vet clean, cfn-lint clean, zero new ruff.** Reported LOWs (user decision, not auto-fixed): `CallWithBearerToken` model-scope inert-but-not-a-bypass (verified via AWS docs; pre-existing auth-template parity), version-pinned deny over-restricting an AIP tier (fails closed), `install.bat` LF-not-CRLF (pre-existing, orthogonal).

### 4th-pass LOW-wave + bypass-suite hardening (user-directed) — COMPLETE ✅
User directed fixing L2 + L4 (NOT L1/L3) and auditing the bypass suite's IAM-glob assertions for match-vs-presence. Detail in `decisions.md` (4th deep-dive → Follow-on) + `spec.md §0 A11` follow-on.

- [x] [coding] **L2** — version-exact entitlement: `entitled_tiers(persona, cris_prefix=…)` probes a tier with its resolved CRIS model id (inference-profile shape) so a version-pinned deny on the tier's own model excludes it → no AIP sourced from a denied model. Cross-tier data-residency fallback keeps version-less probe; budgets version-less; destroy sweeps all tiers (unaffected). | `persona_models.py`, `deploy.py`, `test_persona_models.py` | `TestEntitledTiersVersionExactProbe` (4 cases, teethed). Run: `cd source && .venv/bin/python -m pytest tests/test_persona_models.py tests/test_deploy_personas.py tests/test_budgets_template.py -q`
- [x] [coding] **L4** — destroy unit-test isolation: `_profile()` defaults `personas=[]` + `_run_destroy` mocks `_get_retained_resources` (unmocked per-stack boto3 call). 9 tests 26s→0.4s; whole suite 84s→42s. | `tests/cli/commands/test_destroy_skip_logic.py` | 9 pass in <0.5s. Run: `cd source && .venv/bin/python -m pytest tests/cli/commands/test_destroy_skip_logic.py -q --durations=3`
- [x] [coding] **AUDIT** — bypass suite was presence/substring-based (the weakness that hid the inert global-CRIS glob; test #4 passed throughout the bug). Per user "strengthen the helper itself": rewrote `_shapes_covered_for_keyword` to establish coverage by glob-MATCH (converts #2 + #3 at the source), made #4 match-based, added positive global-Allow-matches test (#5 was negative-only) + `TestSalesDenyMatchesRealModelIds`, unified on one `_iam_glob_match` engine. | `tests/test_persona_policy_bypass.py` | 11 pass; teethed 2 ways (revert global-CRIS fix → #4 + new tests fail; disable inference-profile leading-* → #2/#3 fail). Run: `cd source && .venv/bin/python -m pytest tests/test_persona_policy_bypass.py -q`

**LOW-wave + audit re-gate: Python 1227/0 (+7), Go 10 pkgs ok, go vet clean, cfn-lint clean, zero new ruff. Suite wall-clock 84s→42s.**

## Commit manifest (review-4 handoff hazard)
**~58 files** (untracked incl. parity oracle + bypass fixture; modified incl. the deep-dive + Low-wave files above) — regenerate the exact list with `git status` before commit; full original 48-file core list in `decisions.md`. `git add source/ deployment/ assets/ .gitignore PBAC_README.md` (NOT `git add -u` — that drops the untracked parity oracle / Go persona tests / bypass fixture and silently strips the CI safety nets). Lead handles git per user direction (stay on `rubab-dev1`).

---

## Notes
- **No two tasks in a group write the same file** — verified: Group 3's wizard task owns `init.py`; persona defaults live in a separate `persona_defaults.py`.
- Lambda resolver duplication (separate deploy artifact) is intentional (design §5); parity test covers drift.
- Worktrees not needed — task graph is fully file-disjoint.
- Review scopes (Phase 2 step 12): `python-cli` (config/deploy/package/init/renderers), `go-helper` (credential-process/otel/persona/jwt/config), `infra-lambda` (CFN templates + lambdas), `tests-parity` (all test files). 4 reviewers, one each.
